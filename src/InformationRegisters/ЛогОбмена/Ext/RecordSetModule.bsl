Процедура ПередЗаписью(Отказ, Замещение)
	
	Запрос = Новый Запрос;
	Запрос.Текст = "Выбрать * Из РегистрСведений.КлючевыеФразыЛогаОбмена";
	МассивФраз = Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Фраза");
	
	Для каждого Запись Из ЭтотОбъект Цикл
		Для каждого Фраза Из МассивФраз Цикл
			Если СтрНайти(Запись.Описание, Фраза) > 0 Тогда
				Запись.Группировка = Фраза;
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры
