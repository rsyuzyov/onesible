&НаСервере
Процедура УстановитьНаСервере()
	
	Для каждого ТекущаяСтрока Из Элементы.Список.ВыделенныеСтроки Цикл
		СостояниеРоли = РегистрыСведений.РолиКомпьютеров.СостояниеРоли(ТекущаяСтрока.Компьютер, ТекущаяСтрока.Роль);
		Если СостояниеРоли.Назначена И НЕ СостояниеРоли.Установлена Тогда
			РегистрыСведений.РолиКомпьютеров.ЗапуститьУстановкуРоли(ТекущаяСтрока.Компьютер, Текущаястрока.Роль);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура Установить(Команда)
	
	УстановитьНаСервере();
	
КонецПроцедуры
