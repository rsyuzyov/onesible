Запрос = Новый Запрос;
Запрос.Текст = "ВЫБРАТЬ РАЗЛИЧНЫЕ
               |	ПоступлениеТоваровТовары.АлкогольнаяПродукция КАК Ссылка
               |ИЗ
               |	Документ.ПоступлениеТоваров.Товары КАК ПоступлениеТоваровТовары
               |ГДЕ
               |	ПоступлениеТоваровТовары.Ссылка.Дата >= &Дата
               |	И ПоступлениеТоваровТовары.Номенклатура.Код ЕСТЬ NULL";
Запрос.УстановитьПараметр("Дата", Дата('20191101'));
тз = Запрос.Выполнить().Выгрузить();
Результат = Новый Массив;
Для каждого стр Из тз Цикл
	Результат.Добавить(Сериализовать(стр.Ссылка));
КонецЦикла;
